
// Big O Notation
////////////////////////////////
// Time Complexity

// Constant 0(1) --Fastest
  //Running a statement
    // pop()
    // math
    // property lookups - string.length

// Logarithmic 0(logn) --0(nlogn)
  // discarding partial contents makes it easier over time
  // base 10 - base 2
      // Sort

// Linear 0(n)
  // For loop
  // Unshift
  // Shift

// Quadratic 0(n^2)
  // Double nested loops
     // 0(n^3)
  // Triple nested loops


// Exponential 0(k^n) --Slowest

/////////////////////////////////
// Space Complexity

